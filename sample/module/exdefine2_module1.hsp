/*
	このファイルには拡張定義は記述されていませんが
	同じディレクトリに .cmpexdef ファイルがある場合は
	そのファイル内の定義が有効になります
	
	複数のファイルで同じ定義をする場合は .cmpexdef に記述すると手間が省けます
*/
#module

#defcfunc calc1 int a, int b, int c

	return (a - b - c); * 100

#global
